# NOTE: dependency overrides live in the root package.json under `pnpm.overrides`.
# The `overrides` key here is only honoured by pnpm >=10, and this repo pins
# pnpm 9.15.4 via `packageManager`, so overrides placed here are silently ignored.
packages:
  - "packages/*"
